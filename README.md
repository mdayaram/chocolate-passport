Chocolate Passport
==================

## Setup

Prerequisites
* Ruby version >= 3.3

Run the following commands:

```
$ ./bin/setup
```

## Running Locally

You should be able to just run the application locally with rails server:

```
$ ./bin/rails server
```
